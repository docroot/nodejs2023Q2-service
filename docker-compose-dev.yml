version: '3'

services:
  pgsql:
    container_name: rsschool-pgsql
    build:
      context: ./docker/postgresql
      dockerfile: Dockerfile
      args:
        - DB_USER=${DB_USER}
        - DB_PASSWORD=${DB_PASSWORD}
        - DB_NAME=${DB_NAME}
        - POSTGRES_PORT=${DB_PORT}
        - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    restart: always
    env_file:
      - .env
    environment:
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
      - PGPORT=${DB_PORT}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    ports:
      - ${DB_PORT}:${DB_PORT}
    volumes:
      - pg_data:/var/lib/postgresql/data
    networks:
      - rsschool-network

  nodejs_dev:
    container_name: rsschool-nodejs-dev
    build:
      context: ./
      dockerfile: Dockerfile
      target: dev
      args:
        - PORT=${PORT}
        - DB_PORT=${DB_PORT}
        - DB_USER=${DB_USER}
        - DB_PASSWORD=${DB_PASSWORD}
        - DB_NAME=${DB_NAME}
    restart: always
    env_file:
      - .env
    environment:
      - PORT=${PORT}
      - DB_PORT=${DB_PORT}
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_NAME=${DB_NAME}
    ports:
      - ${PORT}:${PORT}
    volumes:
      - ./src:/usr/src/app/src
      - ./logs:/var/log/rsschool
    depends_on:
      - pgsql
    networks:
      - rsschool-network

volumes:
  pg_data:
    external: true

networks:
  rsschool-network:
    driver: bridge
    name: rsschool-network
